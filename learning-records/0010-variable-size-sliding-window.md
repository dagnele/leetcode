# Variable-size sliding window accepted

The user solved LeetCode 1004 using a variable-size sliding window with `zeroCount` as the constraint state. Future lessons can assume the user understands expanding right, shrinking left while invalid, and updating the best answer only after restoring the window invariant.
