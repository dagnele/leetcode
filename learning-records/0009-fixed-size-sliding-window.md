# Fixed-size sliding window accepted

The user solved LeetCode 643 and 1456 using the fixed-size sliding window pattern: add the incoming element, evaluate when the window reaches size `k`, then remove the outgoing element before continuing. Future lessons can move toward variable-size windows where the left pointer moves to restore a constraint.
