# Product Except Self optimized

The user solved LeetCode 238 first with separate left/right product arrays, then optimized to store left products in the output array and use one rolling `rightProduct` variable. Future prefix/suffix lessons can assume understanding of empty products, forward accumulation, and backward accumulation without division.
