# Is Subsequence accepted with forward scan

The user solved LeetCode 392 with two forward pointers and correctly reasoned that after matching `a` in `abc` against `ah`, the next needed character is `b`. Future teaching can refine this into the cleaner invariant `p1 = matched prefix length` while recognizing the user's accepted approach is correct.
