# Pivot Index solved with running left sum

The user solved LeetCode 724 using `totalSum`, a running `leftSum`, and the relationship `rightSum = totalSum - leftSum - nums[i]`. Future prefix-sum lessons can assume the user understands scanning with a maintained prefix rather than recomputing side sums.
